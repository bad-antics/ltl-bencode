// examples/demo.ltl — runnable demo for ltl_bencode

import std::io
use ltl_bencode as lib

fn main() {
    io::println("ltl_bencode demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}