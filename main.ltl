// ltl-bencode/main.ltl
//
// BitTorrent bencode reader / writer.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-bencode v0.1.0")
    io::println("BitTorrent bencode reader / writer.")
    0
}
